{
    "name": "my-project",
    "version": "1.0.0",
    "scripts": {
      "heroku-postbuild": "npm install --prefix server && npm install --prefix server/client && npm run build --prefix server/client",
      "start": "npm start --prefix server"
    }

    
  }